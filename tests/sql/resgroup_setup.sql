create resource group plgroup with (concurrency=0, cpu_rate_limit=10, memory_limit=30, memory_auditor=1);
create resource group plgroup2 with (concurrency=0, cpu_rate_limit=10, memory_limit=10, memory_auditor=1);
